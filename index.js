const divide =function(){ // defined as a function expression with a name "divide"
    return 2000/100 
}

const square = num =>{ //arrow function with a name "square"
    return num * num
}
const add = (num1,num2)=>{ // arrow function with a name "add"
    return num1 + num2
}